import { createSlice } from '@reduxjs/toolkit'

const initialState = {}

const { actions, reducer } = createSlice({
    name: 'name',
    initialState,
    reducers: {},
    extraReducers: {},
})

export default reducer
